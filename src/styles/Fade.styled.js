import styled from 'styled-components';

export const Fade = styled.div`
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    bottom: 0;
    background: rgba(45, 40, 36, 0.5);
    z-index: 1;
`;
