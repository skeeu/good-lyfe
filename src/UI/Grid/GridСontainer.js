import { styled } from '../../theme';

export const GridСontainer = styled('div', {
    height: '100%',
    width: '100%',

    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignItems: 'center',
    textAlign: 'left',
});
