import { styled } from '../../theme';

export const Fade = styled('div', {
    width: '100%',
    height: '100%',
    position: 'absolute',
    background: 'rgba(45, 40, 36, 0.5)',
    zIndex: 1,
});
